<%- include('../layout', { body: ` <div class="page-header"><h2>\u{1F4CA} Existencias por Almac\u00E9n</h2><a href="/existencias/editar" class="btn btn-primario">+ Ajustar Existencia</a></div>${typeof mensaje !== 'undefined' && mensaje ? '<div class="alerta alerta-exito">\u2705 ' + mensaje + '</div>' : ''}${typeof error !== 'undefined' && error ? '<div class="alerta alerta-error">\u26D4 ' + error + '</div>' : ''}<div class="filtros"><form method="GET" action="/existencias"><select name="almacen_id" class="form-control"><option value="">-- Todos los almacenes --</option>${almacenes.map(a => '<option value="' + a.id + '" ' + (String(filtros.almacen_id) === String(a.id) ? 'selected' : '') + '>' + a.descripcion + '</option>').join('')}</select><select name="articulo_id" class="form-control"><option value="">-- Todos los art\u00EDculos --</option>${articulos.map(a => '<option value="' + a.id + '" ' + (String(filtros.articulo_id) === String(a.id) ? 'selected' : '') + '>' + a.descripcion + '</option>').join('')}</select><button type="submit" class="btn btn-primario">Filtrar</button></form></div><div class="card"><div class="card-body"><table class="tabla"><thead><tr><th>Almac\u00E9n</th><th>Art\u00EDculo</th><th>Cantidad</th><th>Acciones</th></tr></thead><tbody>${existencias.length === 0 ? '<tr><td colspan="4"><div class="vacio"><span class="vacio-icon">\u{1F4ED}</span><p>No se encontraron existencias.</p></div></td></tr>' : ''}${existencias.map(e => '<tr>' + '<td>' + e.almacen_descripcion + '</td>' + '<td>' + e.articulo_descripcion + '</td>' + '<td>' + parseFloat(e.cantidad).toFixed(2) + '</td>' + '<td><div class="acciones"><a href="/existencias/editar?almacen_id=' + e.almacen_id + '&articulo_id=' + e.articulo_id + '" class="btn btn-sm btn-secundario">\u270F\uFE0F</a><form method="POST" action="/existencias/eliminar" class="form-eliminar" onsubmit="return confirmarEliminar(this)"><input type="hidden" name="almacen_id" value="' + e.almacen_id + '"><input type="hidden" name="articulo_id" value="' + e.articulo_id + '"><button type="submit" class="btn btn-sm btn-peligro">\u{1F5D1}\uFE0F</button></form></div></td>' + '</tr>').join('')}</tbody></table></div></div>` }) %>