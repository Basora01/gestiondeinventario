<%- include('../layout', { body: ` <div class="page-header">
    <h2>🏷️ Tipos de Inventario</h2>
    <a href="/tipos-inventario/crear" class="btn btn-primario">+ Nuevo Tipo</a>
    </div>

    ${typeof mensaje !== 'undefined' && mensaje ? '<div class="alerta alerta-exito">✅ ' + mensaje + '</div>' : ''}
    ${typeof error !== 'undefined' && error ? '<div class="alerta alerta-error">⛔ ' + error + '</div>' : ''}

    <form method="GET" class="filtros-barra">
        <div class="filtro-grupo">
            <label>Buscar</label>
            <input type="text" name="busqueda" class="form-control" placeholder="Descripción o cuenta..." value="${filtro}">
        </div>
        <button type="submit" class="btn btn-secundario">🔍 Filtrar</button>
        <a href="/tipos-inventario" class="btn btn-secundario">✖ Limpiar</a>
    </form>

    <div class="card">
        <div class="tabla-contenedor">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Descripción</th>
                        <th>Cuenta Contable</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    ${tipos.length === 0 ? '<tr><td colspan="5"><div class="vacio"><span class="vacio-icon">📭</span><p>No se encontraron tipos de inventario.</p></div></td></tr>' : ''}
                    ${tipos.map(t => '<tr>' + '<td>' + t.id + '</td>' + '<td>' + t.descripcion + '</td>' + '<td>' + (t.cuenta_contable || '—') + '</td>' + '<td><span class="badge badge-' + (t.estado === 'Activo' ? 'activo' : 'inactivo') + '">' + t.estado + '</span></td>' + '<td><a href="/tipos-inventario/editar/' + t.id + '" class="btn btn-sm btn-secundario">✏️ Editar</a></td>' + '</tr>').join('')}
                </tbody>
            </table>
        </div>
    </div>
    ` }) %>